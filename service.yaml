apiVersion: v1
kind: Service
metadata:
  name: osinachiprofile-app
  labels:
    name: osinachiprofile-app-service
    app: website
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    name: osinachiprofile-app-pod
    app: website
